package com.example.androidapp_development.assignment2;

public class signup2MainActivityTest {
/*@Rule
public ActivityTestRule<signup2MainActivity>myActivityTestRule = new ActivityTestRule<signup2MainActivity>();

    @Before
    public void setUp() throws Exception {
    }
    @Test
    public void testUserInputScenario()
    {
        //input text
        String nName = "hi";
        Espresso.onView(withId(R.id.name)).perform(typeText(nName));
        //close soft keyboard
        Espresso.closeSoftKeyboard();
        //perform button click
        Espresso.onView(withId(R.id.subitem)).perform(click());
        //chichking the text

    }

    @After
    public void tearDown() throws Exception {
    }*/
}